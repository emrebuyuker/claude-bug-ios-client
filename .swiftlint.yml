line_length:
  warning: 170
  ignores_comments: true

file_length:
  warning: 800

nesting:
  type_level: 2
  function_level: 2

disabled_rules:
  - type_name
  - trailing_whitespace

excluded:
  - Pods
  - vendor
  - fastlane
  - .backups
  - .bundle
  - build
  - DerivedData
  # Legacy monolithic file — pending refactor into Scenes/Chat/ (see CLAUDE.md)
  - ClaudeBugPoC/ViewController.swift
